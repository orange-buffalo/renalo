---
title: Chat preview
description: Try Renalo's optional conversation selector, message feed, and composer before AI data access is connected.
---

import { Aside } from "@astrojs/starlight/components";
import ScreenshotPair from "../../../components/ScreenshotPair.astro";
import desktop from "../../../assets/screenshots/desktop/33-chat-preview.png";
import mobile from "../../../assets/screenshots/mobile/33-chat-preview.png";

The Chat preview is an opt-in demonstration of Renalo's future conversational interface. A deployment operator enables it and configures LiteLLM as described in [Install with Docker](/getting-started/installation/); when disabled, Chat does not appear in the main navigation.

Renalo uses the configured AI model to replace a new chat's generic title with a concise title derived from its first prompt. The answer content and restored history are still simulated preview data and are not generated from your financial records.

<ScreenshotPair desktop={desktop} mobile={mobile} alt="Renalo Chat preview with a conversation selector, message feed, and composer" />

## Try the interface

- Select **New conversation** to add another conversation.
- Use the conversation selector to move between their separate message feeds.
- Write a message and press **Enter** or select the send button. Use **Shift + Enter** for a new line.
- Renalo echoes the message with placeholder text so the layout can be evaluated with longer responses.

Conversations and messages exist only in the current browser page. Reloading the page clears them.

<Aside type="caution" title="Interface preview only">
  This preview is not connected to LangChain4j, LiteLLM, a language model, or your financial records. Do not treat its responses as analysis and avoid entering sensitive information while it remains an echo demonstration.
</Aside>
